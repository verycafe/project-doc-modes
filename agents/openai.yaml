interface:
  display_name: "Project Doc Modes"
  short_description: "Scaffold categorized local repo docs with PRD -> PHASE -> SPEC and optional SDD-RIPER gates."
  default_prompt: "Use $project-doc-modes SDD to inspect this repository, ask one or two short setup questions at a time, keep generated docs local-only in Git unless I ask otherwise, and scaffold categorized docs under docs/ with PRD -> PHASE -> SPEC structure."
